provider "oci" {
  # Terraform will automatically look for credentials in ~/.oci/config
  # and use the [DEFAULT] profile.
  # You can explicitly set the profile if needed:
  # config_file_profile = "MY_OCI_PROFILE"
}
